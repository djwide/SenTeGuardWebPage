---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="SenTeGuard Blog">
    <section class="section-spacing">
        <div class="space-y-6">
            <h1>SenTeGuard Blog</h1>
            <p class="max-w-2xl">
                Longform updates, research notes, and security insights from SenTeGuard. Posts are published by the team
                and moderated to keep the focus on cognitive security.
            </p>
            <div class="card space-y-3">
                <p class="text-sm uppercase tracking-[0.3em] text-gray-500">Latest Post</p>
                <h2 class="text-2xl text-white">Launching the Cognitive Security Mission</h2>
                <p>
                    This space is reserved for longform posts authored by SenTeGuard. Only authorized team members publish
                    new entries, while readers are welcome to share comments below.
                </p>
                <div class="pt-2">
                    <p class="text-sm text-gray-500">Drafted by SenTeGuard Team</p>
                </div>
            </div>
        </div>
    </section>

    <section class="section-spacing border-t border-gray-900">
        <div class="grid gap-8 lg:grid-cols-[1fr_1.1fr]">
            <div class="space-y-3">
                <h2>Reader Comments</h2>
                <p>Share your thoughts using the alias you prefer. Comments are reviewed before posting.</p>
            </div>
            <form
                class="card space-y-4"
                action="mailto:david@senteguard.com"
                method="post"
                enctype="text/plain"
            >
                <label class="flex flex-col gap-2 text-sm text-gray-400">
                    Alias
                    <input
                        type="text"
                        name="alias"
                        required
                        class="rounded-lg border border-gray-800 bg-black px-3 py-2 text-white focus:border-primary focus:outline-none"
                    />
                </label>
                <label class="flex flex-col gap-2 text-sm text-gray-400">
                    Comment
                    <textarea
                        name="comment"
                        rows="5"
                        required
                        class="rounded-lg border border-gray-800 bg-black px-3 py-2 text-white focus:border-primary focus:outline-none"
                    ></textarea>
                </label>
                <button class="btn" type="submit">Submit Comment</button>
            </form>
        </div>
    </section>
</Layout>
